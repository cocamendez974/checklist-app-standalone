import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'checklist-app-standalone',
  webDir: 'www'
};

export default config;
